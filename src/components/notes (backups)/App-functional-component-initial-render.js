import React, { Component } from "react"
// equivalent to import { default as React, Component } from "react"

// {Component} is unused with functional component

const App = (props) => <div></div>

export default App

// equivalent to export default App = (props) => <div></div>

// equivalent to the class component
